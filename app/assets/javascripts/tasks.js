// $(document).ready(function() {
	// var ad_id = $("#ad_code_id").val();
	// if ($('#ad_code_id').val()) {
		// $.ajax({
			// type : "get",
			// url : '/advertisements/new_upload',
			// data : {
				// ad_id : ad_id
			// }
		// });
	// }
// });

function updateStatus(task_id)
{
	alert("1111");
	alert(task_id);
	alert($("#select" + task_id + " option:selected").text());
	// $("#yourdropdownid option:selected").text();
}
